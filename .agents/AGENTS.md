# Cherry — Project Context (AGENTS.md)

Keep this file up to date: update as the LAST step of every meaningful change. It is the single source of truth for agents working in this repo.

## Mission

Reconstruct a Go server emulator ("Cherry") for LINE PLAY (jp.naver.lineplay.android), LINE Corporation's avatar social game (2012–2024). Initial milestone: the real Android client boots into a usable lobby. The eventual server goal is full game functionality; a successful lobby bootstrap is not completion of that goal. Windows client port comes later.

## Baseline (decided, do not re-litigate)

- APK: `D:\Dev\projects\Cherry\.opencode\line-play-artifacts\LINE+PLAY+-+Our+Avatar+World_10.1.0.0_APKPure.apk`
- v10.1.0.0, versionCode 254, minSdk 23, targetSdk 33. Final release.
- Complete single-file APK: 5179 entries, both arm64-v8a + armeabi-v7a libs.
- `libgame.so` (62,736,024 bytes) has the same uncompressed size as the archived device split. The original size comparison alone did not prove byte identity; compare hashes before relying on that stronger claim.
- Recon found no OBB/Play Asset Delivery requirement; the observed resource model is APK assets plus downloads into app `files/`. No OBB reconstruction is currently justified.
- **Asset policy (2026-09-22):** users download the fullest client, not on-demand streaming. The full santi backup push is the lab instance. Do not build an artsitem/CDN file server unless a later demonstrated blocker needs a file absent from that backup.
- Archive is READ-ONLY. Working copies go in `C:\Users\fes\AppData\Local\Temp\opencode\cherry\` (libs, dex, manifest, resources.arsc already extracted there; agent research artifacts in `...\cherry\tools\`).

## Evidence ledger confidence levels

`reported` → `static-confirmed` (symbol/address verified) → `runtime-confirmed` (captured from client). Unverified findings from the initial recon are `reported` until confirmed.

The 2026-09-20 audit sections below are historical snapshots, not current state. **M0 was achieved 2026-09-21; M1 and M2 were achieved/runtime-confirmed 2026-09-22.** M2 ends at the Garden loading phase, not a usable lobby. The old stopped-lab and gift-spinner handoffs are superseded by the saved UIA console evidence and user-reported visual evidence below. **M3 investigation started; implementation not started.** Follow [PLAN.md ordered checkpoints](PLAN.md#m3-ordered-plan--investigation-started-implementation-not-started). This 2026-09-24 handoff changes documentation only; M3 evidence includes prior captures, current read-only device/binary spot-checks, and user reports, but no new client replay.

**Audit correction (2026-09-20, superseded in part):** AirArmor bypass was never achieved. External capture is not an anti-tamper bypass.

## Architecture (from static recon; verify before coding)

### Client stack
- cocos2d-x 2.2.5, custom NHN layers (`arts`/`ArtsPf`), game ns `Na*`, net layer `XSystem`/`XInNetwork`. No Lua/JS/WebSocket.
- `libgame.so`: 119,419 functions, symbols NOT stripped, not obfuscated.
- JNI bridge: `com.nhnarts.message.PfQueueEvent` (~90 native methods, list in temp `tools\native_methods.txt`).
- Obfuscated DEX class `android.line.s.Service` loads libm0vie/libchromview via runtime-decrypted names (this is why glass DEX parsing fails; strings are plaintext, parser is strict).

### Protocol stacks (static-confirmed 2026-09-21; full reports in temp `cherry\tools\{airarmor,http,lpn}-static\REPORT.md`, main-agent spot-verified)
1. **Native HTTP/JSON** (libcurl static, jsoncpp): base `https://<host>/v4/`. fapi.play.naver.jp (game API), fapi-diary, event, face, gws, terms.line.me, notice2.line.me; CDNs play-static.line-scdn.net / play-static.line-apps.com, obs.line-{apps,scdn}.com. `tx.lbg.play.naver.jp` is an HTTPS API base (billing), NOT an LPN socket. 326 URL builders on `NaDataInvokerProtocol` (list: temp `cherry\invoker_short.txt` if present; addresses in http-static\ADDRESSES.txt).
2. **Session XTCP/JSON** — `session.play.naver.jp:10123` (XPN string, verified at 0x2f818b8). Frame `LO`: `BE32(len+16) "LO" 0x10 flag BE32(payloadLen) BE64(txnId) payload` (payload = JSON control: sendInit sessionId/sessionKey/country/os/lpVer; configure → pingSeconds/noopSeconds). Built by `LPNConnector::Send(raw)` 0x2a186a4 (callers: NaSessionManager::sendInit 0x1b30964, OnPushReqest).
3. **Game gateway LPN** — gws.play.naver.jp, **default port 10000** (`LPN::GetGatewayServerPort` 0x2a17a20 = `return 10000`; only observed caller in `NaSingletonManager::Initialize` at 0x19209cc; internal CreateOption only; no server-exposed override). `/v4/setInitConf.sessionServerInfo` + `SessionInvoker::parseConfigure` @0x1b3b67c configure :10123 only (static-confirmed). Frame (both dirs): `BE32(len = ALL bytes after the length field) | tag | type | BE64(sessionId) | payload`; tag 'G'=game data, 'D','F','L','M','H' variants; type=(agenttype-1)<<3|subtype. Payload = **BE16(msgid)** || protobuf (msgid BIG-endian — corrects early recon; ids namespace-scoped; tables in lpn-static\dispatch_tables.tsv: CIRCLE 55, MONOPOLY 38, LPNP 36, SQUARE 133, GARDEN 82, TLUP 6 entries; outbound GetIndex == inbound dispatch index).
- **Mode 3 (AES-128-CFB128)**: BE32(len) stays PLAINTEXT; bytes from offset 4 encrypted: AES("LP" || BE64(sessionId) || BE16(msgid) || protobuf); EVP re-init per frame (CFB reset), same key+IV both directions; mode 4 = plaintext. Handshake: server 15-byte banner (`00 00 00 08 00 00 …`) → client 11-byte reply `00 00 00 04 00 00 01 0A 35 ED 6A` (order verified from STRB trace 0x2a0c8f0–0x2a0c918; **live vector captured 2026-09-22: `000000040000010a35ed6a`; banner works**) → mode 4 → server `HI` frame `BE32(14) "HI" BE32(keyIndex) BE64(threshold)` → client picks from its 100-entry key table → mode 3; `HO` = stay mode 4. Ping `BE32(2)|'M'|sub` (81='Q', 83='S'; inbound 'M'+85='U' → disconnect); keepalive timeout default 5000ms.
- **Key material**: hardcoded wrapper key ASCII "F609C5FCEFE3F62C6084…DE69C5" (128 chars @0x3008708, first 64 copied; AES-128 key = first 16 ASCII chars) + IV ASCII "B0F9926983812A17DF50A81500E67F34" (@0x3008790) decrypt the server-supplied `arts_session/sckey.enc` blob (**required, not 404-tolerated**) = `[u32][BE64 threshold][100×{BE32 len1,enc1,BE32 len2,enc2}]` → (key64,iv16) pairs; Cherry's generated blob is implemented and accepted by the client.
- **Boot protobuf subset (proto2 optional, has-bits)**: `cs_login_req{1:sessionKey str,2:aid int64,3:version uint32,4:deviceType str,5:languageCode str,6:osType str}` (identical in CIRCLE/MONOPOLY/LPNP cr_login_req); `sc_login_res{1:result,2:enterType}`; `cs_room_enter_req{1:clientMapRevision i64,2:enterType}`; `sc_room_enter_res{1:result,2:npcList,3:clientMapRevision,4:mapFileName,5:playerType,6:circleMemberList,7:skinInfo}`; LPNP `rc_login_res{1:result,2:myroomExist bool,3:myroomTimeout i32}`. NO embedded descriptors (lite runtime); 3877 field statics in lpn-static\field_numbers.tsv.
- **AirArmor enforcement (verified)**: `onAirCallback`@0x2a2b234 (int only) → `SetAirArmorState`@0x200cf9c (write-once latch at +0x4C, only while normal). `IsAirArmorStateNormal`@0x200cf64: abnormal = {1 FAILED_CALLING, 4 ROOTING, 5 SUSPICIOUS_TOOL, 6 REPACKAGED, 9 INSTALLED_TOOL, 10 SUSPICIOUS_FRAMEWORK}; **FRIDA=12, XPOSED=13, DEBUGGER=8 are NORMAL**. Abnormal → ShowAirArmorDetectPopup@0x200cdf0 only (per-frame re-show; button → scene teardown). NO process kill. Callback 12 alone does not latch abnormal; **callback 10 co-fired with Frida and caused [AA-010]**. Keep frida-server stopped in Session B. A clean run without Frida is not a bypass. Detection implementation is inside packed libm0vie.so (entropy 7.8+, invalid JNI_OnLoad bytes; loaded t+0.177s) — not statically extractable; the ptrace self-tracing child (maps only libm0vie) remains unattributed (breakpad is the only proven ptrace user in libgame).

### Key symbols (arm64, from recon; verify image base)
- `LPNAgent::MakeStream` 0x2a11ca0, `LPNAgent::Update` 0x2a10b78
- `NaDispatcherCommon::ResCreateSessionWithToken` 0x1c3c944 (session JSON contract)
- `LP_sc_login_res` CIRCLE 0x2514930 / MONOPOLY 0x16aaf8c
- `LPN::SessionKey` ctor 0x2a19020, Init 0x2a18ff8
- `LPN::AESCrypto::Encrypt/Decrypt` 0x2a191cc / 0x2a19100
- `NaDataInvokerProtocol::baseHostName` 0x1ac4bb8 (host table BSS 0x3BE4590)
- `LPN::LPNConnector::Send` 0x2a18590 / 0x2a186a4
- `ReqCreateSessionWithToken` 0x1c05074, `ReqAccessTokenForGuest` 0x1bbf454
- `LPNAgent::SendPing` 0x2a139e4
- IDA sessions are ephemeral and die: `2f8772ca` and `b61900c0` are stale; the current session may be `d5b16e73`. Enumerate live sessions before use; never treat an id as permanent.

### Auth (static-confirmed 2026-09-21; runtime corrections 2026-09-22)
- LINE SDK v3 embedded (channel-apis.line.naver.jp); channel ID 1350636423. NOT emulating LINE OAuth — local guest route: UI selector **`LD_GUEST`** (string @0x2f8b2e0; NaLoginLayer::MakeLoginButton 0x1cfe130, SNS list-driven layouts SetSnsLoginUITagList 0x1cfe4d0). Guest uniqueCode = device UUID via LoginAdapter.InfoDeviceUUID (saved pref `uuid`, else name-based UUID from ANDROID_ID).
- Guest: POST `https://fapi.play.naver.jp/v4/account/guest/generate` body `{"uniqueCode":"…"}` header `X-LINEPLAY-ACNT = lowercase_md5hex(uniqueCode + "vocky7dkd722i1u8ak84o")` (secret verified @0x2f7d910) → `result.provider` (**"lineplay" on the wire, not "GUEST"**), `result.accessToken`.
- Session: GET `/v4/createSession?nationCode=CC` with cookies cc(=accessToken),expireAt,refreshToken,provider,udid,os,device (+cs unless TW) → `result.{sessionKey,mid,avatarUserId,aid,lineId,lineName,termAge}` (all strings; **aid stays "0" until POST `/v4/create/avatar`**) + **response-header Set-Cookie `AV_AUTH="<quoted value>"`** — client extraction uses `AV_AUTH\t"` … `"` delimiter (string @0x2f84140) and requires value >100 bytes; persisted as the long-term credential; on relaunch cc carries the saved token. AV_AUTH/checkSession round-trip is runtime-confirmed, not an open question.
- **aid "0" routes to avatar creation via `GoToStartScene`; it does not itself suppress Connect.** First login does not self-connect: `create()` sets `+124=1` without calling Connect, and `Create()` is invoked once before the async sckey download. App-foreground `ResumeConnect` produced the working :10123 connection. This is separate from the `+0x171` session-pump gate below.
- Boot: `SplashScene::setupPatcher` → GET `/v4/setInitConf` → msg 10035 (M0). Runtime then GETs `/v4/resource/splash/<ts>/0/JP` and static-CDN `/notice/adr/checkresource2.json`. **Historical checkresource2 404 caused a network-error dialog; it is implemented and no longer the stall.** `CDTMainVersionJson::Request` 0x1b27be8 = `<staticDomain>notice/adr/checkresource2.json`. Parse 0x1becee0: `root["adr"]["10.1.0.0"]` non-empty string, then `root["resource"][<that string>]` with keys animation/he/ch/sound/subui/tx/squareui/ui (atoi, missing→0). No `result` wrapper. Failure → ProcessError → msg 10686. Splash skip fixture: `resourceID: -1` (0 is a different branch). Fresh install cannot skip animation/sound/tx/ui **list** downloads (`RequestVersionIni` 0x1cf9da0). Pack key `"1"` is synthetic — not item-format 1409. checkSession is implemented and accepted on relaunch.
- **TLS (verified — pinning does NOT exist)**: native curl verification fully disabled (`CurlWrapper::InitParam` 0x2a027fc setopt(64,0)+setopt(81,0); repeated in HttpSender 0x2ed3018; no CAINFO/CAPATH) → **any self-signed cert works for all native game hosts**. Java: jadx-verified `LinePlay.setSSLSocketFactory` installs `TLSSocketFactory` = TLSv1.1/1.2 protocol enabler with default platform trust (`SSLContext.init(null,null,null)`); `res/raw/https_keystore` is referenced ONLY by R classes = **dead resource, never used by code**. Earlier "TLS pinning blocks Cherry" claim is dead. (targetSdk 33/no NSC → user CA store untrusted by Java stack — only matters for relayed hosts, which use live infra anyway.)
- Root check: LoginAdapter.checkRootingDevice (non-fatal in practice — game runs rooted).
- `libline-sdk-encryption.so` `Lspg.gk()` mixes APK signing cert + ANDROID_ID with 2 hardcoded secrets — LINE-SDK-login-specific; guest path insulated from it.

### Avatar creation, gift, and cleared preload gate (M2, 2026-09-22)

**Terms/create-flow note (main agent e5e76aef; 2026-09-22):** This is not a missing Cherry route or a server bug: `handleSnsTerms` already returns HTTP 200 + `{"result":true}` for GET and PUT `/v4/social/terms/sns`. Popup type 5 is pushed only by msg 10053 (`result:false`); our GET `result:true` sends 10054 and neither opens nor closes it. Once open, only the client's successful PUT response closes it through `ResAgreedLineTerms`; the observed grey Continue, both-boxes-ticked, and green Continue taps sent no PUT (`cherry-tos.log`, 17:52). Workaround: tap X (local `keyBackClicked`; has sent `PUT /v4/sessionout`); login already continues behind the popup. Do not add another terms endpoint, change `handleSnsTerms`, or try to close the popup from the server. The later accepted flow reached START → login preload → session sendInit → Garden loading; popup work is not the milestone boundary.

- **Avatar creation baseline/runtime-confirmed, dress-up enhancement user-reported working (2026-09-23):** `POST /v4/create/avatar` was accepted by the client before. It accepts `name`, `avatarType`, `nationCode`, `skinColor`, `itemCodes`, `useMid`, optional gzip, returns quoted `AV_AUTH`, and assigns `aid`. The current handler preserves name/country/skin/item codes in memory and returns object-form items; user reports the player-avatar dress-up works beautifully. The exact M/F/A screens exercised were not recorded, so do not claim each sex separately runtime-confirmed. aid remains "0" until this POST.
- **Exact completion URL:** `tutorialDoneURL` 0x1ad4b40 appends protocol-table slot `unk_3BE4D88+0x720`. Static ctor `sub_14E6E48` at 0x14f1b98 writes **`"create/complete"`** (`var_748`, string @0x2f80798) into that slot. Resolved endpoint: **`POST https://fapi.play.naver.jp/v4/create/complete`**, not an unresolved BSS mystery.
- `ReqTutorialDoneWithCachePolicy` 0x1bc5e7c POSTs JSON, omitting `inviteCode` when empty. `ReplaceStep` case 0xD (step 13) calls it after `ShowSystemIndicator`.
- **Historical spinner cause, fixed:** `ErrTutorialDoneWithCachePolicy` 0x1bc612c posts msg 11268 with status 0 for HTTP 400; every other error, including 404, re-calls the request. **404 retries forever.** Success requires `result.status` true (`asBool`) and `result.rewardCoin` int; msg 11268 then calls `HideSystemIndicator` + `ShowCreateWelcomGiftPopup`. `status:false` shows a network-error dialog and retries.
- **Completion implemented/runtime-confirmed:** `handleCreateComplete` returns HTTP 200 + `{"result":{"status":true,"rewardCoin":300}}` for POST `/v4/create/complete`, ignoring the body; non-POST uses existing `notFound`. Covered by `TestCreateComplete`. Console captured POST 200; spinner cleared, 300-Gem gift appeared, and user pressed START. This is no longer an implementation/replay task.
- **After the gift — gate cleared:** START (tag 12) posts 10279 → `changeToLogin`. `NaSingletonManager::Update` calls `NaSessionManager::Update` only when `CSceneManager+0x171 == 1`, set through login preload msg 12210 / the login-complete/world-preload branch (`NaLoginLayer::MsgProc` 0x1d01c44). Previously this left `SetLinkStatus(4)` unconsumed. The captured sendInit + configure reply at `19:42:25.262533` proves that gate cleared and `sendInit` ran; the prior gated-status claim is superseded, not an outstanding transport task.
- **Historical catalog gaps (before 2026-09-23 polish):** SELECT tag 335 at step 7 → step 8 → `ShowStepErrorPopup` string 10077 because `/v4/create/face/setitems/roll` was 404. EDIT was empty because `/v4/create/all/items` was 404. Both GET routes are now coded and the user reports the player dress-up works visually; individual M/F/A coverage was not reported. PERFECT tag 20 at step 8 → step 10 nickname, **not the create POST**; POST happens later at FINISHED.
- **Mascot correction (static-confirmed 2026-09-23):** two onboarding systems were previously conflated. `NaExperienceOfApp` is the first “help Cherry pick my outfit” closet: four local `cherry_closet` intro images lead to a real local avatar clone (`1155131794639385601`) equipped from hardcoded item codes; it does not call `/v4/avatar/<id>`. `NaCreateLayer::InitCloneAvatars` is the later player-avatar tutorial and requests four server avatars (`1151262555912366100`, `1151262590612422506`, `1151262602112441105`, `1151262571612391402`). Their authentic item lists were not found in the archives; Cherry now serves explicitly synthetic, device-backed M/F/A profiles for those exact IDs (unit-tested; player flow user-reported working, individual profile coverage unrecorded). Do not use those fixtures to repair the first Cherry closet.

### Implemented preload fixtures and crash history (2026-09-22)

Current `http.go` GET routes return HTTP 200 with these exact bodies (non-GET uses existing `notFound`; query parameters do not alter the fixtures):

| Route | Exact response body |
| --- | --- |
| `/v4/setting/all` | `{"result":{"notiFlag":false,"changeCountry":false,"countryName":"","soundConfig":false,"notiConfig":{},"privacyConfig":{},"roomSize":{"max":0,"cur":0}}}` |
| `/v4/brand/list` | `{"result":[]}` |
| `/v4/buddy/list/type/0` (query ignored) and prefix `/v4/sync/friends/<timestamp>` | `{"result":{"existProfile":false,"nextCursor":0,"timestamp":"0","friendsCount":0,"buddyList":[],"newbieRecommendList":[],"nearbyRecommendList":[],"bookmarks":{}}}` (`friendSyncBody`) |
| `/v4/line/buddy/v4/list` | `{"result":{"nextCursor":0,"buddyList":[]}}` (`friendLineBuddyBody`) |

- **Validation/deployment:** `TestPreloadStubs`; Go tests passed and `cherry.exe` was rebuilt before replay. Brand shape has an array-shape regression assertion. These are prior implementation results, not tests/builds run during this documentation update.
- **First preload failure:** generic “Failed to get friend information” came from `/v4/setting/all` 404 through shared `NaDispatcherPreload::ErrPreloadJob` msg 12206. That message does not necessarily identify a friend endpoint.
- **Wrong-brand crash, fixed:** initial `{"result":{"buddyList":[]}}` was invalid for `ResFriendBrandBuddyWithCachePolicy_PL` @ `0x1c31bd0`, which expects `result` itself as an array. Object size 1 followed by numeric indexing caused JsonCpp `strcmp(..., NULL)` and repeatable GLThread SIGSEGV (device `11:25:38.891`, `11:25:53.075`, `11:27:04.516`). Fixed to `{"result":[]}` with the regression assertion above.
- **Post-fix host sequence (runtime-confirmed, host clock only):** `%TEMP%\opencode\uia-windows.txt:790–820`: setting/all 200 `19:42:25.020863` → brand/list 200 `.102264` → gmAvatarList **wrong object 200** `.235467` → additionalInfo object 200 `.257805` → sendInit/configure `.262533` → disconnect `.789970`. This is separate from the fixed 11:25/11:27 wrong-brand crash.
- **Fatal evidence (runtime-confirmed, device clock only):** `%TEMP%\opencode\cherry\tools\m3-evidence\db_hits.txt:9–11`: GLThread SIGSEGV / SEGV_MAPERR at `0x0`, `11:42:25.074`, `11:43:22.931`, `11:43:59.051`; exit-info deaths `11:42:25.464`, `11:43:23.291`, `11:43:59.405`, SIG11. **No reliable historical sub-100ms clock synchronization:** latest prior sample has device ~0.75s behind host UTC; historical ~0.32s lag is only a medium-confidence inference from disconnect-to-death, assuming they track each other. Timezone conversion is not synchronization. Earlier “sendInit then crash” was narrative: fatal-before/after gm or sendInit is NOT established.
- **Postmortem limitation (prior independent checks):** all six `nelodmp` files have only 13 nonzero bytes each, no PC/backtrace; `/data/tombstones` contains only old Bluetooth evidence. `crash_dump64` fails on this translated ARM64 emulator. **Runtime cause remains unknown.**

### M3 investigation handoff (2026-09-24)

- **Strong static candidate, NOT a proven runtime cause:** `NaDispatcherCommon::ResGMAvatarAid` @0x1b749b0 reads `result.size()` then indexes numerically; `Json::Value::operator[](int)` @0x2aee088 branches to numeric @0x2aedd88; `CZString` comparator @0x2aeaa34 calls `strcmp`. A parsed string-key object versus a numeric NULL key can null-deref on GLThread. Historical gmAvatarList object-200 is mechanically dangerous, but no fault PC links it to these crashes. `ResAvatarAdditionalInfo` @0x1b45230 uses string keys and is lower priority.
- **First diagnostic, not new implementation:** existing `http.go:340–349` numeric-ID guard returns 404 for named gmAvatarList and additionalInfo. Follow PLAN steps 1–3: USER-operated replay of unchanged current code with passive host Cherry console + adb logcat capture; verify actual gm 404 and compare crash/no-crash. The guard affects both paths and other changes since historical runs limit attribution. No guessed gm body, shotgun 404, preference reset, agent restart or replay; keep Frida stopped.
- **Scene/gateway separation (static-confirmed):** `NaGardenScene::init` @0x1fbb9e8 loads the map locally; `ChangeDepth` @0x1fcb79c starts the Garden enter connector. Gateway is not needed to draw the scene, but room-enter data is required to hide loading via `NaGardenScene::OnConnectCompleted` @0x1f965ec. A Garden loading sprite proves neither room entry nor a usable lobby. No evidence gateway absence causes SIGSEGV. Only after repeatable crash survival, follow PLAN steps 4–5 for non-destructive routing and progressive handshake/GARDEN login/room entry.

### Assets
- APK `assets/` = 2873 files (12.3MB), plain PNGs, no PVR/CCZ. Organized dump `lineplay-original-assets/android-10.1.0.0-2024/` == APK assets extracted.
- Historical downloaded set (archived device backup `lpbackup/.../files/`, 14,005 files / 359MB): UIResource 7557/7557, animation 4616/4616, sound 483/483 — all MD5-verified complete vs manifests in `files/version/*.ini` (format: newline `MD5_HEX_UPPER:/relative/path`; `<category>lastversion.ini` = u32 LE version). Path mapping: `/X.plist`→UIResource/, `/X.png`→animation/, `/BGM_x.ogg`→sound/. These archive counts are not the size of the current full santi restore.
- **Current device (2026-09-22):** full santi backup (1.1 GB) pushed into `/data/data/jp.naver.lineplay.android/`, owner `u0_a234` / 10234, `restorecon` done. `files/` now contains animation/UIResource/item/etc.; the old empty-files/no-version baseline is superseded. Do not assume archived version values equal the restored device's values without checking.
- **Welcome animation fixed:** missing `files/animation/ac_motion.aniproj` caused the intro stall; that project plus 51 PNGs are now on device from the backup. APK `assets/animation/` only has cherry_closet, so the APK alone did not supply ac_motion. This is separate from the bubble NPC and the now-cleared gift spinner.
- **First Cherry closet assets (static/device-confirmed 2026-09-23):** `NaExperienceOfApp::InitUI` hardcodes 12 starter codes; ten resource-backed custom/dress dirs with `1409_iteminfo.artsitem` and renders exist in santi/on device. `BD`/`SD` map to unsupported categories and are not missing-download faults. Of 48 hardcoded alternatives, the user accepts only four original complete render sets: `CUHA0036Z` (224404139), `CUON004TV` (225106259), `CUSH00267` (225302815), `CUAH004JH` (225405885); these remain device-local and are not being substituted. `CUON004TV` has no original `dp.png` in any archive, so its closet list thumbnail may be blank; its render set exists. Other unavailable alternatives remain unsupported.
- `.ast` = SQLite (`localString(stridx INTEGER UNIQUE, local TEXT)`). `.artsitem` = semicolon CSV, variable fields. `.avb/.avc` custom skeleton/frame binaries (reader in libgame.so). `.artsg*` = 2014 fixed-width catalogs.
- Item textures: recon counted only 191 item dirs in historical lpbackup; catalogs are server-served. The 2014 iOS dump (`lineplay-original-assets/ios-mini-4.3-2014/item.zip`) has 2364 dirs. Its whole `iteminfo.artsitem` directories are not confirmed compatible with the 10.1 `1409_iteminfo.artsitem` parser; matching-ID original `dp.png` previews are compatible and scale from 140×140 to 130×100 in `NaCreateItemCell`. Fifty such previews are restored for the curated creation lists; manifest/hashes: `C:\Users\fes\AppData\Local\Temp\opencode\cherry\dressup\dp_restore.tsv`. Missing cosmetics cannot be assumed harmless to a required avatar/scene until tested.
- **Pre-M3 dress-up asset scope (2026-09-23; acceptance updated 2026-09-24):** user accepts only the four original, fully renderable Cherry closet alternatives `CUHA0036Z`, `CUON004TV`, `CUSH00267`, `CUAH004JH`; the other 44 hardcoded choices remain missing/partial and are not substituted. All four metadata/render sets are on device. `CUON004TV` lacks an original `dp.png` across all archives, so its closet list thumbnail may be blank. **User-reported:** first-launch Cherry closet intro works flawlessly with expected missing cosmetics; individual thumbnail/equip combinations remain unrecorded, not a pending overall closet replay. For player-creation catalog items, 50 same-ID original `dp.png` files were restored from the read-only 2014 zip (25 custom, 25 dress); 2 more thumbnails already existed. iOS previews are 140×140 and scale to 130×100. Main agent spot-verified sample SHA-256, uid `u0_a234`, and `app_data_file` labels. Full manifest and staged tar: `C:\Users\fes\AppData\Local\Temp\opencode\cherry\dressup\dp_restore.tsv` and `dp_restore.tar`. No 2014 `iteminfo.artsitem` was imported.
- **Asset delivery policy:** this curated set uses device-local storage. If the offered catalog grows substantially or serves many separate clients, centralized Cherry item-file serving would avoid repeated per-device pushes and improve update/distribution scale; only add it when that need is measured and complete, authorized originals exist. A CDN cannot recover the missing 44 originals or `CUON004TV` thumbnail.
- Client resource flow: checkresource2 JSON (no result wrapper) → category `arts_<cat>_ini_<ver>/update_<lang>.ini` (probe `[]`, never 0-byte) → files. Backup lastversion u32 LE: animation 531, sound 530, subui 430, tx 540, ui 540 describe archived lpbackup, not a current device inventory. Lang suffix `jp` vs `ja` is a first-run probe. Preserve protocol knowledge, but do not recreate on-demand streaming under the full-client policy.
- Splash skip (static-confirmed): `ResCheckSplashDownload` 0x1b7d39c — serve `{"result":{"update":false,"resourceID":-1,"resourceImageCount":0,"resourceImages":[]}}`.
- **Completeness is unverifiable:** assume the restored archive is missing assets. There is no way to verify completeness — players never obtained every gacha item, furniture, or other catalog asset. A file's absence from the santi backup or the APK is not proof it never existed. Do not start building a CDN for those gaps unless a later demonstrated block requires a specific missing file.

## Tooling rules

- Prefer `idalib` MCP tools for native analysis; `glass` only where idalib is incompatible. Glass APK parsing failed while lifting classes2.dex (`Invalid string in class name`); that failure alone does not prove deliberate obfuscation is the cause. Python/androguard were used for DEX recon; artifacts are in temp `tools\`.
- android-mcp tools for device interaction; glass frida-* tools for instrumentation.
- Temp scratch dir: `C:\Users\fes\AppData\Local\Temp\opencode\` (cherry\ = extracted APK parts; cherry-asset\ = asset dissection).
- For DEX/Java work prefer `jadx-mcp`; Androguard is the fallback if `jadx-mcp` is unavailable.
- Use short, bounded commands (normally 10 seconds for status probes). No unbounded boot loops, repeated multi-minute tshark retries, or background commands that retain the shell's output handles. Use `workdir` rather than cmd `cd`; use dedicated read/edit/search tools.
- The main model can inspect images returned by tools. The prior claim that it cannot see screenshots is obsolete. If a tool fails, record the tool error rather than infer the UI from logs alone.

## Historical verified AirArmor evidence — bypass still unresolved

Artifact root below: `C:\Users\fes\AppData\Local\Temp\opencode\cherry\`.

- `aa-capture\logcat-game.txt:32`: `AirArmor: Phase = RELEASE` in the captured game process (PID 20374).
- `aa-capture\logcat-game.txt:270`: epoch `1789854802.613`, `Air Callback:AIR_CALLBACK_DETECTED_FRIDA,12,Frida`.
- `aa-capture\logcat-game.txt:272`: epoch `1789854804.612`, `Air Callback:AIR_CALLBACK_SUSPICIOUS_FRAMEWORK,10,Frida`.
- `aa-capture\logcat-game.txt:274`: `AIR_CALLBACK_DONE` follows those detections. DONE is a completion callback, not proof of a clean result or bypass. Detection appeared roughly 56 seconds after process creation; an immediate smoke test is insufficient.
- `tools\airarmor-evidence.txt:330–374` records a historical self-tracing pair: parent PID 10431 had `TracerPid:10472`; child 10472 had `PPid:10431`, `TracerPid:0`, and a wait4 syscall. This is evidence of a ptrace relationship. Its exact role in failed Frida attaches is not yet isolated.
- The child mapped `libm0vie.so` but not `libgame.so`/`libchromview_android.so`. This makes the early libm0vie loading stage a candidate watchdog origin, not a proven detection function. Do not kill the child or patch a guessed address on that inference alone.
- Recon identified result-handling symbols: `PfQueueEvent_onAirCallback` at `0x2a2b234`, `NaExceptionManager::SetAirArmorState` at `0x200cf9c`, `IsAirArmorStateNormal` at `0x200cf64`, `ShowAirArmorDetectPopup` at `0x200cdf0`. Silencing result UI would not necessarily remove ptrace ownership or native inspection.
- `tools\airarmor-diagnose.js` exists, explicitly says diagnostic-only and not exercised in LINE PLAY, and does not change return values. No saved successful game-hook/bypass run was found. Earlier SystemUI attach/script success is only a Frida infrastructure smoke test.
- Berberis is a separate instrumentation issue: host x86_64 code and translated ARM64 guest code are different hook targets. Direct ARM64 `libgame` Interceptor support has not been demonstrated. Do not attribute every failed script/session to AirArmor.

## Historical Task 3 evidence — partial startup capture (2026-09-20 audit)

- Independently re-read `aa-capture\boot.pcap`: **168,152 bytes, 435 records**, Linux SLL2; SHA-256 `b48ce1f0f925baab0fb0fef4fc7c9977298092b64b07dce0ad2389a96e1b5fed`.
- Saved supporting files: `aa-capture\sockets.txt`, `logcat-game.txt`, `dns-server.log`. The capture is device-wide; do not attribute every packet to LINE PLAY. Socket sampling started after launch and does not attribute every short-lived startup flow.
- First observed DNS queries include Facebook/Adjust SDKs, then `fapi.play.naver.jp` at epoch `1789854749.555`, `lan3rd.line.me` at `1789854749.871`, `play-static.line-scdn.net` at `1789854750.784`, and `media-pu.line-scdn.net` at `1789854761.337`. Responses for fapi/play-static/media-pu were NXDOMAIN in this capture.
- Complete captured ClientHellos contain SNI for `graph.facebook.com`, `app.adjust.com`, and `lan3rd.line.me`, all on port 443. The earlier `pcap2.py` SNI parser incorrectly read the hostname length without skipping `name_type`; its blank SNI output is invalid. It also skipped IPv6. Audit script `C:\Users\fes\AppData\Local\Temp\opencode\cherry-status-audit.py` independently parses DNS/IP/TLS with dpkt and a checked server-name decoder. It does not perform TCP reassembly; one captured IP record is too short to decode.
- `logcat-game.txt:234–247` records GET `https://lan3rd.line.me/v1/lineplay/android?notificationLocalRv=0&noticeNewTerm=3`, status 200. This is an original LINE notice service response, **not a Cherry response** and not M0.
- The same run logs language changes (`en`, then `ja`) and `NoticeNotificationActivity` at lines 260–264, followed by AirArmor detections. `setAppLanguageType` does not prove it stalled at a language-selection popup. A separate earlier screenshot did show language selection; do not conflate separate runs.
- Neither `session.play.naver.jp` nor `tx.lbg.play.naver.jp` appears as a DNS query in this capture. This is a scoped absence, not proof they are never used or that the exact blocking gate is known. Loopback ports in the capture are not established LPN ports.
- **Missing:** actual game API request paths/methods/headers/bodies, evidence linking URL builders to the observed requests, startup response contracts, complete gate order, and the LPN endpoint/port. The original Task 3 acceptance criteria are not met.

## Historical Task 5 evidence — partial DNS work, no M0 at the 2026-09-20 audit

All state statements in this section describe that audit only. They do not describe the running Go server/device or supersede achieved M0/M1/M2.

- Historical `aa-capture\dns-server.log` shows A/AAAA sinkhole handling during a run launched with `-dns-server 192.168.1.7`. `gws.play.naver.jp` was queried at 13:11:13. A DNS query alone does not demonstrate further UI/protocol progress.
- Host experiment: `C:\Users\fes\AppData\Local\Temp\opencode\cherry-dns.py`, UDP 53, game-domain suffixes -> `10.0.2.2`, other queries relayed to `1.1.1.1`. At audit, PID 3020 still owns UDP 53 and a bounded host-local DNS query returns `10.0.2.2` for fapi. This temporary script is not production-ready or durable configuration.
- **Device routing is currently not working:** `ping -c 1 -W 1 fapi.play.naver.jp` returns `unknown host`. The current emulator command line has no `-dns-server` override. No emulator restart/routing change was performed during this audit.
- The prior conclusion that Android 17/Bionic ignores `/etc/hosts` universally is unsupported. The failed experimental hosts replacement had SELinux label `shell_data_file`; labeling, mount namespaces, or resolver behavior could explain failure. Current `/etc/hosts` is stock, labeled `system_file`, containing only localhost entries.
- No host TCP listener on 80 or 443 was found during audit. Repository contains no Go implementation. No successful local TLS handshake, validated trust modification, game request received by Cherry, or client-accepted Cherry response is documented. **Task 5 remains partial; M0 is not achieved.**

## Historical environment and baseline validation (2026-09-20 audit)

The process, launch-command, filesystem, and tool-error observations below are historical, not current lab state.

- Host: Windows; Java/Python/Node available. Glass executable `C:\Apps\Glass\glass-v0.5.0-windows-amd64.exe` uses Frida 17.9.5. Recon found SDK `build-tools\36.0.0\apksigner.bat` and `zipalign.exe`; do not assume signing tools are absent.
- **Live audit state (2026-09-20):** `emulator-5554` is online, Android 17/API 37, ABI list `x86_64,arm64-v8a`, native bridge property `libndk_translation.so`. Historical game logs identify Berberis 16.0.0. ADB currently runs as shell (uid 2000); neither LINE PLAY nor frida-server has a PID. Glass reports `attached:false`.
- Current emulator command: `emulator.exe -netdelay none -netspeed full -avd Cherry` (QEMU equivalent also checked). No explicit GPU or DNS override; effective GPU configuration was not revalidated in this audit. Do not describe an old launch command as current state.
- **Historical graphics workaround:** boot failed with `IllegalArgumentException: No config chosen` on API 30 and 37; `-gpu swiftshader_indirect -no-snapshot-load` allowed native rendering and a language-selection UI. The exact rejected EGL attributes were not captured; the earlier RGB565 diagnosis was speculative. Keep the known-good launch flags for future controlled tests.
- **Historical baseline:** `Cocos2dxRenderer SurfaceCreated nativeInit:true` and an earlier screenshot establish rendering, not login/lobby or an anti-tamper bypass. Old Android-11 app-data backup: `C:\Users\fes\AppData\Local\Temp\opencode\lineplay-data-backup.tar`.
- Previously computed SHA-256: APK `aa6495a0430beef0c9a719a1f09fe836ff1c82a0f4f96235d47fffda070b45d1`; arm64 libgame `d824c3ef72ac1948f3c5a31619879384f4659c9306e6f5a1b04793334e795b04`. Other native hashes are in `tools\airarmor-evidence.txt`. Signing-certificate fingerprint was not completed in the visible Task 1 work.
- **Frida:** `/data/local/tmp/frida-server` exists (110,825,032 bytes, executable) but is not running at audit. No re-push is currently required. Host copy: `C:\Users\fes\AppData\Local\Temp\opencode\frida-server`. Raw Glass scripts previously reported `Java is not defined`; bridge loading must be handled explicitly. The compiled bridge file is not evidence of successful loading into LINE PLAY. Prior failed scripts also used obsolete Frida API forms; do not treat those failures as protection evidence.
- **android-mcp:** device enumeration works; current Snapshot fails with UIAutomator `ApplicationSharedMemory not initialized`. This is a tool/runtime error, not evidence about LINE PLAY's UI or model vision. The foreground activity from ADB is the launcher. No game launch, Frida attach, reboot, root toggle, TLS change, or anti-tamper modification was performed during the audit.
- glass MCP has no frida-server install verb (that's GUI-only, `crates\glass-frida\src\server.rs`); server lifecycle is manual as above. Glass source available at `D:\Dev\projects\Glass` for reference.

## Current status (2026-09-24)

- **M0 ACHIEVED (2026-09-21 02:41, runtime-confirmed)**: client `GET /v4/setInitConf?nationCode=us&appVer=10.1.0.0` → 200 consumed (follow-ons: `/v4/resource/splash/<ts>/0/JP` — nationCode JP from OUR fixture — and play-static `/notice/adr/checkresource2.json`). Logs: `C:\Users\fes\AppData\Local\Temp\opencode\cherry-m0c.log`.
- **M1 ACHIEVED (2026-09-22, runtime-confirmed):** guest generate → createSession → avatar creation; force-close/relaunch skips login via checkSession + persisted AV_AUTH. `social/terms/sns` `{"result":true}` cleared the login loop. Patcher, string table (`arts_tx` 00543 record grammar), diaryskin `[empty]`, and UIResource gates are cleared, not pending device probes.
- **M2 ACHIEVED (2026-09-22, runtime-confirmed):** avatar creation + completion/gift + transition through login preload/session initialization into the Garden loading phase. **This is not a usable lobby and does not complete the project's initial lobby goal.**
- Go server: repo root, stdlib only (`main.go`, `http.go`, `dressup.go`, `cert.go`, `dns.go`, and their Go tests). Listens HTTPS :443 (dual RSA-2048 + ECDSA-256 certs, **explicit CipherSuites incl. static-RSA AES-GCM/CBC** — the client's ancient OpenSSL offers ONLY static-RSA suites; Go 1.22+ default suites fail it; the `tlsrsakex` GODEBUG is REMOVED in Go 1.27 but explicit CipherSuites still works), HTTP :80 and HTTPS :443 share the mux. **Implemented:** checkresource2, splash, terms, diaryskin, strings, sckey, createSession, checkSession, create/avatar, create/complete, dress-up fixtures, and setting/friend preload fixtures listed above. :10123 banner + live hello + sendInit + configure response are runtime-confirmed. :10000 gateway conflict remains deferred.
- **Recurring notice suppression implemented (2026-09-23):** `dns.go` sinks game hosts plus exact `lan3rd.line.me` to `10.0.2.2` on UDP :53 (AAAA returns empty) and relays other queries to `1.1.1.1:53` (4s). Bind failure is nonfatal. TLS `GetConfigForClient` rejects exact `lan3rd.line.me` before HTTP. Tests pass; the historical lab Python resolver is no longer running. User-reported visual suppression was with Python DNS; no separate controlled acceptance record isolates the embedded DNS behavior. No synthetic notice response or cache mutation.
- **Dress-up polish implemented (2026-09-23); player flow and first-launch Cherry closet accepted (user-reported, 2026-09-24):** `dressup.go` serves GET `/v4/create/face/setitems/roll` (two M/F/A sets), GET `/v4/create/all/items` (sex-specific categories, per-sex DRESS/SET), and native multipart POST `/v2/create/face/analyze` (deterministic response; uploaded image ignored, not real analysis). `http.go` stores submitted name/type/skin/country/item codes in memory and returns typed item objects via `/v4/avatar/<numeric-id>`. Four tutorial IDs receive synthetic device-backed M/F/A looks, not claimed authentic; named avatar paths no longer misroute as numeric IDs. Human MFACE/FFACE intentionally empty; animal AETC empty (no verified FE render+preview pair). Four animal bottom/shoe parts are set-element-only without list previews. Fifty original previews were restored; Cherry's four original alternatives remain local and are not added to creation APIs. `imgPath` stays empty. Prior independent checks: gofmt, Go tests, race tests, vet, build passed. User confirms closet intro works flawlessly with expected missing cosmetics; previous “closet replay pending” is stale. Individual M/F/A and thumbnail/equip coverage remain unrecorded; do not upgrade user reports to exhaustive runtime capture.
- **Lab launcher (2026-09-23):** repo-root `start-emulator.bat` starts AVD `Cherry` with the known-good flags (`-netdelay none -netspeed full -gpu swiftshader_indirect -no-snapshot-load -dns-server 192.168.1.7`). It requires `cherry.exe` running (embedded :53 sink) and the host LAN IP to be edited if it changes. Verified by a non-launching test copy; the emulator itself was not started by the main agent.
- **Cherry closet replay helper (2026-09-23):** repo-root `replay-cherry-closet.bat` checks that LINE PLAY is stopped, backs up `Cocos2dxPrefsFile.xml` to `/data/local/tmp/`, changes only `EXPERIENCE_OF_APP` to false, verifies the change, and preserves the preference file inode/owner/SELinux label by copying edited contents back in place. It never launches the app. An isolated copy of the batch was run on a temporary XML file: first run set false, second run detected already false. During that test the real preference remained true with the same SHA-256 before/after; the later successful closet report does not establish whether the helper was used. The user launches LINE PLAY manually; exiting the closet sets the flag true again. No preference reset is planned for M3.
- **M2 user-operated acceptance:** intro animation → CREATE → Female → SELECT (error dialog dismissed) → EDIT (empty) → PERFECT → name → FINISHED; `POST /v4/create/avatar` 200 and `POST /v4/create/complete` 200. Spinner cleared, 300-Gem gift appeared, user pressed START, and setting/friend preload jobs advanced. **User-reported visual evidence:** the user visibly saw a sprite image reading **“Garden”** immediately before the crash. “Garden” is a visual report, not an HTTP string or proof of a usable scene.
- **Session acceptance evidence (live console, host time):** `19:42:25.262533 SESSION lo ... op=0x00 txn=1 payload={"country":"","lpVer":"adr 10.1.0.0","os":"adr 17","sessionId":"1","sessionKey":"..."}`; at the same timestamp, `SESSION reply ... op=0x10 txn=0 bytes=56` (server configure response). This proves login preload msg 12210 / `CSceneManager+0x171` gate cleared and `sendInit` ran; it does not establish a usable lobby.
- **Saved evidence:** latest UIA console dump `C:\Users\fes\AppData\Local\Temp\opencode\uia-windows.txt`, first replay lines 773–820: 775 create/complete, 790–817 preload/sendInit, 820 disconnect; relaunches continue after 822. Earlier Garden-load failures have an unknown cause and no usable PC/backtrace. Historical host/device clocks were not reliably synchronized; timezone conversion does not establish whether the fatal preceded or followed gm/sendInit. Android fatal logcat times and postmortem limitations are recorded in the crash history above. Console capture is not a packet capture; the Garden sprite report is explicitly user-supplied. No fresh runtime evidence was collected for this documentation-only handoff.
- **Build/deployment:** Go tests passed and executable was rebuilt before the accepted preload replay. Earlier stale-executable history (03:39 binary versus 14:15 `http.go`) remains a warning: future code changes must run `gofmt`, `go test ./...`, and **`go build -o cherry.exe .`**; tests alone are not deployment. Restart wipes in-memory accounts. User controls restarts/replays; do not script taps or restart processes automatically.
- **AirArmor [AA-010] is a UI blocker, not a process kill.** Callback 12 (DETECTED_FRIDA) is "normal"; callback 10 (SUSPICIOUS_FRAMEWORK, data string `"Frida"`) is abnormal and shows `お使いのデバイスでは安定的なサービスの提供が困難なためアプリをご利用いただけません。 [AA-010]` / 確認 → scene teardown. Cause: **frida-server running on the device**. Mitigation: stop it during client runs; binary renamed to `/data/local/tmp/.fs_archive` (host copy still at `C:\Users\fes\AppData\Local\Temp\opencode\frida-server`). After kill, a relaunch (pid 7426) had **no** Frida/framework callbacks. Callback 11 SUSPICIOUS_NETWORK is also "normal" (does not latch).
- **YunDetectService.exe is Baidu (Pan Baidu downloader), unrelated to LINE PLAY. Keep it running.** Main agent rechecked PID 29356 listening on `127.0.0.1:10000`. QEMU maps guest `10.0.2.2:10000` onto that loopback port. Cherry currently has only a :10000 observer, not gateway protocol support. **After crash survival only:** propose Cherry binding specifically to host LAN IP:10000 and exact `gws.play.naver.jp` DNS A→LAN IP; verify guest TCP reachability and narrowly scoped firewall access while YunDetect remains running. No server-exposed gateway port override was found; session config is :10123 only. Never kill/disable YunDetect; no routing change now. HTTP :80/:443 are unaffected.
- **M3 — investigation started; implementation not started.** Goal: survive the GLThread null-address crash and reach a usable Garden/lobby. Follow [PLAN.md](PLAN.md) ordered checkpoints: prepare passive capture and record build identity/paired clock samples → one USER-operated current-build baseline replay (user restarts Cherry, then manually launches LINE PLAY; confirm actual gm 404 and record crash/no-crash plus visible progress) → branch and establish repeatable crash survival → non-destructive gateway routing → progressive handshake/GARDEN login/room enter. Prepare capture before launch; no code/fixture/routing changes before this baseline. This is not a controlled one-variable comparison with historical runs: the guard affects both named paths and other intervening changes limit attribution. Preserve M0–M2, accepted player dress-up/first-launch closet with optional cosmetic gaps, full-client asset policy, read-only archive, Frida stopped, YunDetect running and user control of every restart/replay. No guessed gm body, shotgun 404, preference reset or CDN/asset-server work. This handoff changes docs only: no process/device/code changes, launch/restart or commit.
- **Other unresolved requests, not selected culprits:** the historical burst included 404s for chat room bg/sync, noti reddot, shop/status, push/regist, items/modified, playhome lp_rmchat, preliminary/winners, inven/counts, coin GEM/CASH, vip balance/lounge, avatar popup except, pet arrange, movie list, fashionista hint, composite list, banned keyword, and `/v4/setting/Android/0`. Historical gmAvatarList/additionalInfo wrong-object 200s now become 404 under the numeric-ID guard; actual next-run responses must be captured. gmAvatarList is the strong static candidate above, not a proven runtime cause. Neither status nor uncertain cross-clock ordering establishes causation.
- Historical patcher reports: temp `cherry\tools\patcher-static\`. Their IDA session `b61900c0` is stale; sessions die (possible current id `d5b16e73`, enumerate before use). Prefer jadx-mcp over Androguard when JADX is up. Artifacts are out of git history, force-pushed and gitignored; LFS was abandoned. Archive remains read-only.
