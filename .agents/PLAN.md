# Cherry — Current Plan (PLAN.md)

M3 investigation underway; implementation NOT STARTED. Architecture, evidence ledger, exact response bodies, and history: `.agents\AGENTS.md`. This update is documentation-only.

## Status (2026-09-24)

- **M0 COMPLETE — runtime-confirmed 2026-09-21:** client accepted Cherry's boot configuration.
- **M1 COMPLETE — runtime-confirmed 2026-09-22:** guest login reaches avatar creation; checkSession + persisted AV_AUTH works on relaunch.
- **M2 ACHIEVED / COMPLETE — runtime-confirmed 2026-09-22:** avatar creation + completion/gift + transition through login preload/session initialization into the Garden loading phase. **Not a usable lobby; the project's initial lobby goal remains unmet.**
- **Dress-up accepted by user:** player-avatar flow works (individual M/F/A coverage unknown); first-launch Cherry closet intro now works flawlessly. Missing alternative cosmetics are expected/accepted; scope remains four original complete alternatives.
- **Main-agent read-only lab snapshot:** `emulator-5554` authorised/running; LINE PLAY has no PID; repo was clean before documentation edits. No automatic app/Cherry launch.

## M2 acceptance and handoff evidence

- User completed avatar setup; console recorded `POST /v4/create/avatar` 200 and `POST /v4/create/complete` 200. Spinner cleared, 300-Gem gift appeared, and user pressed START. Setting and friend preload jobs advanced. **User-reported visual evidence:** a sprite image reading **“Garden”** appeared immediately before the crash; this is not an HTTP string.
- At host `19:42:25.262533`, console captured `SESSION lo ... op=0x00 txn=1 payload={"country":"","lpVer":"adr 10.1.0.0","os":"adr 17","sessionId":"1","sessionKey":"..."}` and `SESSION reply ... op=0x10 txn=0 bytes=56` (server configure response). This proves preload msg 12210 / `CSceneManager+0x171` gate cleared and `sendInit` ran; the former gated-status claim is superseded.
- `TestPreloadStubs` covers the implemented preload fixtures, including the corrected brand `result` array regression. Go tests passed and executable was rebuilt before replay; not rerun during this documentation handoff.
- Historical replay A, **host clock only**: `19:42:25.020863` setting/all 200 → `.102264` brand/list 200 → `.206209` buddy 200 → `.235467` gmAvatarList **200 with object** → `.257805` additionalInfo object 200 → `.262533` sendInit + configure reply → `.789970` SESSION disconnect. UIA console: `C:\Users\fes\AppData\Local\Temp\opencode\uia-windows.txt`, lines 790–820 (gm 801, additionalInfo 812, session 815–816, disconnect 820). Replays B/C likewise show gm 200 and disconnect.
- **Device clock only:** GLThread SIGSEGV / SEGV_MAPERR at `0x0`: `11:42:25.074`, `11:43:22.931`, `11:43:59.051`; exit-info deaths `11:42:25.464`, `11:43:23.291`, `11:43:59.405`, status 11. Host UTC+8/device GMT conversion does NOT synchronize clocks. Today's device lag was ~0.75s; historical ~0.32s lag is only a medium-confidence inference from death/disconnect differences (0.315–0.326s), assuming disconnect tracks death. **Neither fatal-before-gm/sendInit nor gm/sendInit-before-fatal is established.** Earlier “sendInit then crash” prose was narrative, not causal ordering.
- **Cause unknown; no postmortem PC:** `crash_dump64` failed on translated ARM64 state in the x86_64 Android 17 emulator. No LINE PLAY tombstone; all six `nelodmp` files contain only 13 non-zero bytes (MDMP header/zero directory), no modules/backtrace. Evidence: `%TEMP%\opencode\cherry\tools\m3-evidence\` (`db_hits.txt:9–11`, `scan_dumps.py`). Earlier fixed brand-shape crashes at device 11:25/11:27 are separate.

## M3 ordered plan — investigation started; implementation NOT STARTED

1. **Prepare passive evidence for the next USER-operated replay.** Retain Cherry console, host-side logcat captures (`-b all -v threadtime` and `-b all -v epoch`), and post-run `dumpsys activity exit-info`. Record run ID, APK/Cherry version/hash, timestamps and paired host/device clock samples; keep each clock's ordering separate. Store raw logs in temp, not docs (headers/session payloads contain credentials). Capture may still yield no PC. **Checkpoint:** capture ready before launch, binary identity recorded; no code/fixture/routing change.
2. **First low-risk experiment: user restarts Cherry, then manually launches LINE PLAY with current unmodified code.** Compare the historical object-200 runs with the existing numeric-ID guard (`http.go:340–349`; `dressup_test.go` assertions), which now returns 404 for gmAvatarList and additionalInfo. Confirm the actual gmAvatarList 404 in the new console and record crash/no-crash plus visible progress. Treat this as a current-build baseline replay, not a controlled one-variable comparison with historical runs: add no further intervention; the guard affects both named paths and other changes since historical runs limit causal attribution. If gm is still 200 or the request is absent, resolve deployment/replay comparability before drawing conclusions; do not invent a gm fixture.
   - **Why first:** static `ResGMAvatarAid` @ `0x1b749b0` uses `result.size()` and numeric indexing. A nonempty string-key object can reach JsonCpp `strcmp(key, NULL)` on GLThread. Mechanically sufficient, but **hypothesis only without PC/backtrace**. AdditionalInfo uses string keys/optional pet list and is lower priority.
3. **Branch on that replay.**
   - **A — crash gone:** repeat user-operated runs at least twice total with the same build; record observation duration/progress beyond the old failure point before attribution. Preserve 404 unless a demonstrated blocker needs gm success. If needed, verify the full native parser/error path before adding exact `{"result":[]}` plus a regression test; no guessed fields. **Checkpoint:** repeatable survival and preserved M2/dress-up flow, not merely one launch without a crash. Then proceed to step 4.
   - **B — crash persists:** retain first-fault logs and request/consumer ordering within each clock; do not merge uncertain host/device timelines. Inspect ONE next response consumer selected by evidence. If a PC is indispensable and passive capture still lacks it, seek approval for a native ARM64 device/image run with the same APK/Cherry; no Frida client instrumentation by default. **Checkpoint:** a captured PC/backtrace or a verified single-consumer hypothesis with a bounded next test, not shotgun 404 fixtures. Stay here until crash survival is repeatable.
   - **Both branches:** preserve M0–M2, brand-array/numeric-ID guards, dress-up/assets, notice suppression and session fixes. No account/app-data/preference reset; the requested Cherry restart already loses in-memory accounts. User controls every subsequent replay/restart.
4. **Only after crash survival: route gateway without touching YunDetect.** `gws.play.naver.jp:10000` is hardcoded at boot; `CreateOption` is internal, not a Cherry response knob. `/v4/setInitConf.sessionServerInfo` configures only :10123. Propose a specific host LAN-IP:10000 Cherry bind instead of wildcard (additional bind only if feasible), exact gws A-record override to that LAN IP, other currently sunk hosts kept at `10.0.2.2`, and narrowly scoped firewall permission if needed. **Checkpoint:** actual guest TCP connection reaches Cherry's observer while YunDetect still owns `127.0.0.1:10000`; DNS LAN reachability alone is insufficient. No routing changes now.
5. **Then implement minimal gateway/GARDEN entry, one verified packet at a time.** Current :10000 is only an observer; :10123 session configure is not a gateway implementation. Verify framing/handshake, GARDEN login and room-enter IDs/fields against recovered protobuf contracts and native consumers; implement/test each transition before advancing, without speculative payloads. Future code changes require format/tests/build, followed by user-controlled replay. **M3 acceptance:** loading indicator disappears and map/avatar are interactive (`NaGardenScene::OnConnectCompleted` path); repeat stability and retain M2. A Garden loading sprite alone is NOT completion. The scene/loading UI is client-local; gateway absence has not been shown to cause the GLThread crash.

## Pre-M3 dress-up polish (2026-09-23; acceptance updated 2026-09-24)

- `dressup.go` implements GET `/v4/create/face/setitems/roll`, GET `/v4/create/all/items`, and the native multipart POST `/v2/create/face/analyze` (deterministic response; uploaded image is ignored, not analyzed). `http.go` preserves submitted name/type/skin/country/items in the in-memory account and returns item-object profiles for `/v4/avatar/<id>`. The four tutorial IDs get explicitly synthetic M/F/A profiles because authentic outfits are absent from archives. Human MFACE/FFACE are empty by user decision; animal AETC remains empty because no CAFE item has both 10.1 render assets and an authentic preview. Four animal bottom/shoe parts are renderable but only included as set elements, not list cells, due missing previews.
- First-launch Cherry's closet remains separate and client-local. **User now reports the intro works flawlessly; closet replay is no longer pending.** Missing alternative cosmetics are expected/accepted: only `CUHA0036Z`, `CUON004TV`, `CUSH00267`, `CUAH004JH` have complete original render sets; no replacements for the other 44 choices. `CUON004TV` has no original archived `dp.png`; the report does not establish every individual thumbnail/equip combination.
- Device-store strategy: 50 original, matching-ID 2014 `dp.png` thumbnails were restored (25 custom + 25 dress); 2 more catalog thumbnails already existed. Source: read-only `lineplay-original-assets/ios-mini-4.3-2014/item.zip`; the 140x140 originals scale to 130x100. Manifest/hash list and temporary validation helper: `C:\Users\fes\AppData\Local\Temp\opencode\cherry\dressup\dp_restore.tsv` and `deploy_dp.py`. No whole 2014 item directories were imported: `iteminfo.artsitem` compatibility with the 10.1 `1409_iteminfo.artsitem` parser is unverified. Local curated storage is simplest here; centralized item-file serving would scale better if many clients need a larger changing catalog, but is not needed for this bounded scope.
- Repo-root `replay-cherry-closet.bat` backs up and flips only `EXPERIENCE_OF_APP=false` when the app is stopped; it does not launch LINE PLAY. Historical isolated-copy test changed only temporary XML; live preference stayed `true` with identical SHA-256. The later successful closet report does not establish whether this helper was used. Exiting the closet marks the flag true again; no preference reset is planned for M3.
- Prior independent main-agent checks passed: `gofmt -l .`, `go test -count=1 ./...`, `go test -race -count=1 ./...`, `go vet ./...`, `go build -o cherry.exe .`; thumbnail hashes/owner/SELinux labels spot-checked. No tests/build or agent-initiated restart/replay in this documentation update. Player-avatar and closet acceptance are user reports, not exhaustive per-screen validation.

## Guardrails and explicitly deferred work

- **User controls all restarts/replays; no scripted taps or automatic restarts.** Restarting Cherry wipes in-memory accounts. Future code changes require format, tests, and executable rebuild; passing tests alone is not deployment. The earlier milestone handoff was documentation-only; the pre-M3 dress-up polish above changed Go code and device thumbnails, without app/process restarts or M3 crash investigation.
- **Full-client asset policy:** users download the fullest client; the santi backup push is the lab instance. Do not mimic on-demand streaming or build an artsitem/CDN file server unless a later demonstrated blocker requires a file absent from the backup. Archive stays read-only. **Assume the archive is missing assets.** There is no way to verify completeness — players never obtained every gacha item, furniture, or other catalog asset. A file's absence from the santi backup or the APK is not proof it never existed. Do not start building a CDN for those gaps unless a later demonstrated block requires a specific missing file.
- **Keep Frida stopped.** Instrumentation deferred unless a specific client-state question requires it. Record AirArmor callback 10 if seen; do not press its popup button. Callback 12 alone is harmless.
- **Keep YunDetectService.exe running; defer :10000 routing until step 4.** Latest main-agent probe: `127.0.0.1:10000` LISTENING, PID 29356. Do not kill/disable it or change routing for this task. Do not reopen the `/etc/hosts` experiment; emulator DNS override is the known-working route.
- Deferred: LINE OAuth, billing, ad SDK stubs, OBS media, FCM, crash collectors, minigames, chat, social/economy, full item catalog + 2014 import, all-messages protobuf recovery, persistent storage, Windows client port, and Frida instrumentation.
