{
  "$schema": "https://opencode.ai/config.json",
  "references": {
    "line-play-artifacts": {
      "path": "./line-play-artifacts",
      "description": "Line PLAY archive backup of assets and .apk files. READ-ONLY. If need to dissect files, copy to %temp% and work with that copy instead."
    }
  }
}
